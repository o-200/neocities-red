name: CI

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  lint:
    name: Lint (ubuntu-latest)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Ruby 4.0.0
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 4.0.0
          bundler-cache: true

      - name: Debug versions
        run: |
          ruby -v
          bundle exec rubocop -V

      - name: Run linter
        run: bundle exec rubocop

  test_linux:
    name: Test (ubuntu-latest)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Ruby 4.0.0
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 4.0.0
          bundler-cache: true

      - name: Run tests
        run: bundle exec rspec

  test_non_linux:
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Ruby 4.0.0
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 4.0.0
          bundler-cache: true

      - name: Run tests
        run: bundle exec rspec

  freebsd:
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    name: Test (freebsd-14.2)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run lint and tests in FreeBSD VM
        uses: vmactions/freebsd-vm@v1
        with:
          release: "14.2"
          usesh: true
          prepare: |
            pkg install -y ruby rubygem-bundler ca_root_nss git
          run: |
            ruby -v
            bundle -v
            bundle config set frozen true
            bundle install
            bundle exec rubocop
            bundle exec rspec
